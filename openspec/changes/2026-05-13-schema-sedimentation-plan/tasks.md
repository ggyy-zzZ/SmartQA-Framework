# Tasks: Schema 沉淀方案流水线

- [x] `LearningSinkPolicy` + `ActiveLearningService.learnWithSinkPolicy`
- [x] `SchemaSedimentationPlanService`（方案 JSON、digest、流水线）
- [x] `SedimentationPlanPrompts` + `POST /qa/mysql/sedimentation/pipeline`
- [x] `MysqlSchemaCatalogService.exportCatalogWithConnection(DynamicConnection)` 无硬编码上限重载
- [x] `openspec/design/schema-sedimentation-plan-pipeline.md` + `spec.md` Scenario + `project.md` / `AGENTS.md` 索引
- [x] `KnowledgeAssistantPrompts` 文本块改为拼接字符串（兼容低版本 javac 的构建环境）
- [x] `SchemaSedimentationPlanServiceTest`（`extractJsonObject`）
